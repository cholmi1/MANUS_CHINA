# 다중 워크시트 및 사진 썸네일 검증 기록

로그인된 체크리스트에서 첫 상담 기록에 PNG 테스트 사진을 첨부했고 화면에서 사진 카드와 삭제 동작을 확인했다. 초기 초소형 PNG는 이미지 라이브러리에서 손상된 것으로 판정돼 썸네일에서 제외됐고, 유효한 PNG를 새로 생성해 다시 첨부했다. 같은 사용자 기록으로 보호된 `fieldRecords.export` API를 호출한 결과는 HTTP 200, XLSX MIME 형식, 1개 기록, 17,176자 Base64 데이터였다. 사진 첨부 이전 내보내기보다 결과 크기가 증가했다. 실제 생성 XLSX를 다시 열어 `요약`, `업체·담당자`, `부스·제품사양`, `가격·납기`, `인증·샘플`, `기타` 시트가 생성됐고, `업체·담당자` 시트에 현장 사진 이미지 객체 1개가 삽입됐음을 확인했다. 검증에 사용한 두 사진은 사용자 기록에서 모두 제거했다.

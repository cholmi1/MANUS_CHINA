# Netlify 런타임 안내 검증

개발 환경의 상담기록 화면에서 `field-ledger:api-proxy-error` 이벤트에 502 상태를 전달해 Netlify API 프록시 장애 시나리오를 모의 실행했다. 다음 화면 점검에서 안내 팝업의 제목, 상태 코드, 닫기·새로고침 동작을 확인한다.

모의 502 이벤트 후 `서비스 연결을 확인해 주세요.` 제목, 프록시·백엔드 점검 안내, 상태 코드 502, `닫기`와 `새로고침` 제어가 포함된 팝업이 표시됐다. `닫기`를 선택하면 기존 상담기록 화면으로 정상 복귀했다.

업체별 엑셀 요청의 네트워크 응답을 지연해 진행 상태를 실행했으며, 뮤테이션의 `isPending` 상태를 기반으로 전체 화면 로딩 카드와 버튼 비활성화·`엑셀 생성 중` 문구가 렌더링되도록 구현했다. 브라우저 자동 점검은 완료된 응답 상태를 캡처해 로딩 카드 자체를 기록하지 못했으므로, 해당 조건은 코드 경로 및 타입·자동 테스트로 추가 검증한다.

375px 모바일 화면에서 상담기록의 업체별 엑셀 제어, 업체 폴더 입력과 빈 상태 레이아웃이 유지되는 것을 확인했다. 전체 안내 오버레이는 최대 폭과 모바일 패딩을 별도로 지정해 작은 화면에서도 화면 중앙에 안전하게 표시되도록 구성했다.

`VITE_APP_ID`와 `VITE_OAUTH_PORTAL_URL`을 제거한 별도 Vite 미리보기에서 상담기록 화면을 열었다. 백엔드 프록시가 없는 독립 미리보기에서는 API 연결 안내가 예상대로 먼저 표시됐고, 배경에 로그인 배너와 로그인 제어가 존재하는 것을 확인했다. 프록시 안내를 닫은 뒤 환경 변수 누락 로그인 안내를 확인한다.

프록시 안내를 닫고 로그인 버튼을 누르자 OAuth 페이지로 이동하지 않고 `로그인 설정이 필요합니다.` 팝업이 표시됐다. 팝업에는 누락된 `VITE_OAUTH_PORTAL_URL`, `VITE_APP_ID` 변수와 Netlify 환경 변수 설정·재배포 절차가 명시돼 있으며, 닫기와 확인 제어도 표시됐다.

두 공개 OAuth 값을 제공한 별도 미리보기에서도 백엔드 프록시 부재에 따른 API 안내를 닫아 로그인 배너를 준비했다. 다음 단계에서 `requestAnimationFrame`을 잠시 보류해 실제 OAuth 이동 전에 로그인 진행 오버레이와 버튼 비활성화 상태를 확인한다.

OAuth 이동 전 프레임을 일시 보류한 뒤 로그인 버튼을 누르자 버튼이 `연결 중`으로 바뀌고 비활성화됐으며, 중앙에 회전 아이콘·`LOGIN CONNECTION`·`보안 로그인 연결을 준비하고 있습니다.` 문구가 포함된 진행 오버레이가 실제로 표시됐다.

인증된 개발 화면에서 업체별 엑셀 요청의 네트워크 응답을 보류하자 내보내기 버튼이 `엑셀 생성 중`으로 바뀌고 비활성화됐으며, `FIELD EXPORT`·`업체별 상담기록을 엑셀 파일로 정리하고 있습니다.`·사진 썸네일 및 캡션 안내가 포함된 중앙 진행 오버레이가 실제로 표시됐다.

보류된 내보내기 화면에서 `엑셀 생성 중` 버튼이 렌더링된 것을 확인했다. 브라우저 콘솔의 반환값이 자동 기록되지 않아, 이어서 버튼의 DOM 속성을 화면 접근성 레이블로 노출해 실제 disabled 값을 확인한다.

`엑셀 생성 중` 버튼의 DOM `disabled` 값은 `true`로 확인됐다. 이 상태에서 중앙 `FIELD EXPORT` 진행 오버레이도 함께 표시돼, 중복 내보내기 방지와 진행 안내가 동시에 동작함을 확인했다.

OAuth 값이 있는 미리보기에서 API 안내를 닫아 로그인 제어를 다시 준비했다. 로그인 진행 상태에서 같은 방식으로 DOM `disabled` 값을 확인한다.

로그인 클릭 후 버튼이 `연결 중`으로 전환되고 중앙 `LOGIN CONNECTION` 진행 오버레이가 다시 표시되는 것을 확인했다. 다음으로 DOM `disabled` 값을 접근성 레이블로 노출해 확인한다.

`연결 중` 버튼의 DOM `disabled` 값은 `true`로 확인됐다. 로그인 진행 오버레이와 중복 클릭 방지 상태가 함께 적용됨을 실제 브라우저에서 확인했다.

환경 변수 누락 미리보기에서 다시 표시된 초기 API 안내를 닫아 로그인 설정 안내의 확인 제어를 검증할 준비를 마쳤다.

로그인 설정 안내의 `확인` 제어를 실제로 눌렀고, 팝업이 사라져 로그인 배너와 상담기록 화면으로 정상 복귀했다.

# 다업체 상담 기록 검증

로그인된 상담기록 화면에서 첫 업체 폴더에 `검증용 Shenzhen Nova Tech`, 담당자 `Jin Park`, 부스 `S1-208`을 입력했다. 폴더 생성 후 업체 목록에 표시됐고, 선택한 업체 제목 아래에 8개 상담 항목과 업체 전용 사진 첨부 영역이 열리는 것을 확인했다. 다음 단계에서 첫 상담 메모·사진을 저장하고 두 번째 업체와의 분리 상태를 검증한다.

첫 업체의 첫 상담 항목에 MOQ·단가 확인 메모를 입력하고 저장 요청을 실행했다. 저장 완료 후 업체 폴더 수와 해당 항목의 저장 상태를 확인한다.

첫 업체의 메모가 `이 업체 폴더에 저장됨` 상태로 전환된 것을 확인했다. 새 업체 폴더 작성 상태로 전환해 동일한 상담 항목이 두 업체 사이에서 독립 저장되는지 검증한다.

두 번째 검증 폴더 `검증용 Yiwu Horizon Materials`(담당자 `Mei Lin`, 부스 `B2-116`)를 입력하고 생성 요청을 실행했다. 저장 완료 시 두 업체 목록 및 새로 선택된 업체의 빈 상담 기록 상태를 확인한다.

두 번째 폴더가 생성되어 업체 목록이 2개가 됐고, 새로 선택된 업체에는 첫 업체의 메모가 표시되지 않았다. 두 번째 업체 첫 상담 항목에 생산 리드타임·샘플 조건 메모를 입력했으며, 저장 후 두 폴더를 오가며 내용이 유지·분리되는지 확인한다.

두 번째 업체의 메모도 `이 업체 폴더에 저장됨`으로 전환됐다. 다음으로 첫 업체를 다시 선택하여 첫 메모가 유지되고 두 번째 메모가 표시되지 않는지 확인한다.

첫 업체를 다시 선택했을 때 Jin Park의 MOQ·단가 메모만 표시되고 Mei Lin의 생산 리드타임 메모는 표시되지 않아 폴더 간 분리를 확인했다. 선택된 업체의 8개 상담 항목마다 JPG/PNG/WEBP 전용 파일 입력이 활성화된 것도 확인했으며, 다음 단계에서 첫 항목에 PNG를 첨부한다.

첫 업체의 첫 상담 항목에 PNG 업로드를 시작했다. 숨겨진 파일 입력은 첨부 라벨을 대상으로 작동하며, 업로드 요청이 `사진 업로드 중` 상태로 전환되는 것을 확인했다. 업로드 완료 후 사진 미리보기·캡션·삭제와 다른 업체와의 분리 상태를 이어서 확인한다.

PNG가 첫 업체의 첫 상담 항목에 저장되어 미리보기, 파일명, 캡션 입력, 삭제 제어가 표시됐다. 캡션에 `검증용 첫 업체 상담 화면`을 입력했고, 포커스를 이동해 서버 저장을 완료한 뒤 새로고침과 다른 업체 선택으로 지속성과 분리를 확인한다.

사진 캡션의 포커스를 이동한 뒤 새로고침했으며, 첫 업체의 메모·사진·입력한 캡션이 모두 유지됐다. 이제 두 번째 업체를 선택해 첫 업체 사진이 보이지 않는 것을 확인한 뒤, 테스트 폴더와 첨부 사진을 정리한다.

두 번째 업체를 다시 선택했을 때 Mei Lin의 메모만 표시되고 첫 업체의 PNG와 캡션은 표시되지 않아 사진까지 포함한 폴더 분리를 확인했다. 두 번째 검증 폴더 삭제를 시작했으나 화면 동작 확인 도중 시간이 초과되어 현재 확인 후 안전하게 정리를 재개한다.

브라우저 세션 오류로 화면 삭제 확인을 계속할 수 없어, 사용자 ID 1과 정확한 검증용 업체명으로만 범위를 제한하여 두 폴더를 데이터베이스에서 정리했다. 외래 키 연쇄 삭제 후 검증용 업체·상담·사진 메타데이터 수가 모두 0건임을 확인했다. S3 원본은 고아 키가 되지만 데이터베이스 및 UI에서는 접근할 수 없다.

새 `vendors.export` 경로는 업체 폴더의 상담 행을 기존의 카테고리·업체 워크시트 형식으로 평탄화하되, 사진 썸네일·파일명·캡션을 그대로 포함하도록 연결했다. 타입 검사와 9개 자동 테스트를 통과했고, 브라우저에서 빈 업체 목록의 `업체별 엑셀` 다운로드 요청과 성공 알림도 확인했다.
